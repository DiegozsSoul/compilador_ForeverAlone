programa foreveralone;
var
 float i, j, p;
 float Arreglo[10], OtroArreglo[10];
 float valor;
 
funcion int fact(int j)
var int z;
{z = j + (p - j * 2 + j);
si(j == 1) entonces
 {regresa(j);}
 sino
 {regresa(j*fact(j-1) );}
}

funcion void inicia(int y)
 var int x;
{ x = 0;
  mientras(x < 11) haz
 {Arreglo[x] = y * x;
  x = x + 1;}
}

principal()
{lee(p); j = p * 2;
 inicia(p * j - 5);
 desde z=0 hasta 9 hacer
      {Arreglo[z] = Arreglo[z] * fact(Arreglo[z] -p);}
 desde z=0 hasta 9 hacer 
       {OtroArreglo[z] = Arreglo[z] - p;}
  mientras(z>=0) haz
     {escribe ("resultado", Arreglo[z], fact(z+2)*valor);
       z=z-1;
     }

  mientras(z<10) haz
     {escribe ("Otros datos", OtroArreglo[z], p, z + OtroArreglo[z]);
       z = z + 1;
     }
}