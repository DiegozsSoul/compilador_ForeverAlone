programa foreveralone;
var int i, j,p,l,k;
int Arreglo[10];
int  OtroArreglo[10];


funcion int inicia(int y)
 var int x, z;
{ x = 5;
  z = 1;
  x = x * z + y;
  escribe(x*z);
  Arreglo[z] = x * y * 2;
  regresa(x);
}

principal()
{ lee(j);
  i = inicia(j);
  p = 1;
 escribe('TEST');


 escribe('Este es p',p);
 escribe('Este es arr',Arreglo[1]);
}

