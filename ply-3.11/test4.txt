programa foreveralone;
var int i, j,p,l,k;

funcion int inicia(int y, int n)
 var int x, z;
{ x = 5;
  z = 1;
  x = x * z + y *n;
  escribe(x*2);
  regresa(x);
}

principal()
{ lee(j);
  lee(k);
  i = inicia(j,k);
  p = 1;
 escribe('TEST',i);

 escribe('Este es p',p);

}




























