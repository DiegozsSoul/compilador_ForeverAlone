programa foreveralone;
var int i, n, w;
funcion int patito(int a, int b)
var int x;
{
x=1;
x = a*b;
regresa(x);
}

funcion int test(int l)
var int k;
{
k=1;
 mientras(l > 0) haz
  {
   k = patito(k,l);
   l = l - 1;

  }
  regresa(k);
}

principal()
{ i = 1;
  lee(n); 
 w = test(n);
 escribe(w);
}

