programa foreveralone;
var int i, n, w;
funcion int patito(int a, int b)
var int x;
{
x=1;
escribe('Aqui',a,b);
x = a*b;
escribe(x);
escribe(a*b);
regresa(a*b);
}

funcion int test(int l)
var int k;
{
k=1;
 mientras(l > 0) haz
  {
   escribe(k,l);
   k = patito(k,l);
   l = l - 1;
  regresa(k);
  }

}

principal()
{ i = 1;
  lee(n); 
 w = test(n);
 escribe(w);
}

